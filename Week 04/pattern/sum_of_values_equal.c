#include <stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    int a[n];
    int sum = 0;
    scanf("%d", &sum);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &a[i]);
    }
    for (int i = 0; i < n; i++)
    {
        }
    return 0;
}